//TODO: Write function to wrap cal error
/* use crypto_layer::common::error::{CalError, CalErrorKind};
use crypto_layer::prelude::*;
use neon::prelude::*;

pub(crate) fn js_error_from_cal_error<'a>(
    cx: &mut impl Context<'a>,
    err: CalError,
) -> JsResult<'a, JsUndefined> {
    Ok(cx.undefined())
}
 */
